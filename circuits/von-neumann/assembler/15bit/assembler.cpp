#include <stdio.h>
#include <iostream>
#include <fstream>
#include <string>

int main (int argc, char*argv[]) {
  std::ifstream myfile;
  //myfile.open();

  printf ("adresse: %s", argv[0])

  return 0
}
